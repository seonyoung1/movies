import React from 'react';

const Search = () => {
    return (
        <div>
            <form>
                <input type="text" value="" />
                <button type="submit">Search</button>
            </form>
        </div>
    );
};

export default Search;
